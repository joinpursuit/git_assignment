const sumMode = require('./math-module.js')
const stringMode = require('./strings-module.js')
//sumMODE is an object with the funcion sum so we have to call it as such

console.log(sumMode.sum(1,2))
console.log(sumMode.multiply(2,2))
console.log(sumMode.divide(2,2))
console.log(sumMode.square(5))
console.log(stringMode.bigSmall("this is a big string of stuff"))
